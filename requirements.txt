# DSA Coach Dependencies
# =======================

# Core (required)
python-dotenv==1.2.1

# Rich terminal UI (recommended for better experience)
rich==14.2.0

# Advanced CLI input with multiline editing (recommended)
prompt-toolkit>=3.0.0

# Syntax highlighting (required by prompt-toolkit for code highlighting)
pygments>=2.0.0

# LLM Providers (install at least one)
openai==2.14.0
anthropic==0.75.0

# Testing
pytest==9.0.2
pytest-asyncio==0.24.0

# Agent Framework
pydantic-ai==0.3.0
pydantic>=2.0.0

# Async SQLite
aiosqlite==0.21.0# Development Tools (Linting & Formatting)
ruff==0.14.10
mypy==1.19.1
pre-commit==4.5.1
